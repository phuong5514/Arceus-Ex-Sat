<!DOCTYPE html>
<html lang="vi">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            Chi tiết: <%= current_class._id %>
            <!-- Chi tiết -->
        </title>
        <link rel="stylesheet" href="/css/styles.css">
    </head>

    <body>
        <div class="flex-column m-16 gap-8">
            <div class="flex-row space-between">
                <h1> <%= current_class._id %></h1>
                <button onclick="window.location.href='/class'">Quay lại</button>
            </div>
            <div class="grid box column-gap-4 bg-grey-100 align-self-center align-center px-32 pt-16 pb-32 min-w-600">

                <div class="grid-colspan-3"></div>
                <h2 class="grid-colspan-6 text-center">Thông tin lớp học</h2>
                <button type="button" class="grid-colspan-3 h-full" onclick="window.location.href='/class/edit/<%= current_class._id%>'">Thay đổi</button>
                <div class="grid-colspan-12 message-panel" id="message" style="display: none;"></div> 
                <label class="grid-colspan-4 text-left font-bold border-right ">Mã lớp học</label>
                <div class="grid-colspan-8">
                    <p class="grid-colspan-8 w-full border-box"><%= current_class._id %></p>
                </div>
                <label class="grid-colspan-4 text-left font-bold border-right ">Mã khóa học</label>
                <div class="grid-colspan-8">
                    <p class="grid-colspan-8 w-full border-box"><%= current_class.course_id %></p>
                </div>
                <label class="grid-colspan-4 text-left font-bold border-right ">Năm học</label>
                <div class="grid-colspan-8">
                    <p class="w-full border-box">
                    <%= current_class.academic_year %>
                    </p>
                </div>
                <label class="grid-colspan-4 text-left font-bold border-right ">Học kì</label>
                <div class="grid-colspan-8">
                    <p class="w-full border-box">
                        <% if (current_class.semester === "1") { %>
                            Học kỳ 1
                        <% } else if (current_class.semester === "2") { %>
                            Học kỳ 2
                        <% } else if (current_class.semester === "summer") { %>
                            Học kỳ Hè
                        <% } %>
                    </p>
                </div>
                <label class="grid-colspan-4 text-left font-bold border-right ">Giảng viên</label>
                <div class="grid-colspan-8">
                    <p class="border-box w-full"><%= current_class.lecturer %></p>
                </div>
                <label class="grid-colspan-4 text-left font-bold border-right ">Tổng số sinh viên</label>
                <div class="grid-colspan-8">
                    <p class="border-box w-full"><%= current_class.max_students %></p>
                </div>
                <label class="grid-colspan-4 text-left font-bold border-right ">Lịch học</label>
                <div class="grid-colspan-8">
                    <p class="w-full border-box">
                    <%= current_class.schedule %>
                    </p>
                </div>
                <label class="grid-colspan-4 text-left font-bold border-right ">Phòng học</label>
                <div class="grid-colspan-8">
                    <p class="w-full border-box">
                    <%= current_class.classroom %>
                    </p>
                </div>
                <label class="grid-colspan-4 text-left font-bold border-right">Ngày tạo</label>
                <div class="grid-colspan-8">
                    <p class="w-full border-box"><%= formatted_created_at %></p>
                </div>
            </div>
        </div>
    </body>
</html>